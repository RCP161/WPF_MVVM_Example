<catel:UserControl
    x:Class="Company.Basic.UI.PersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:catel="http://schemas.catelproject.com">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>


        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Text="Name" />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Text="{Binding Name}" />

        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Text="Surname" />
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Text="{Binding Surname}" />

        <TextBlock
            Grid.Row="2"
            Grid.Column="0"
            Text="LogIn" />
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            IsReadOnly="True"
            Text="{Binding User.DisplayText, Mode=OneWay}" />
    </Grid>

</catel:UserControl>
